import { FC, ReactNode } from 'react';
declare type Props = {
    children: ReactNode;
    onVisibleChange: (isVisible: boolean) => void;
    className?: string;
    config?: IntersectionObserverInit;
};
declare const Observer: FC<Props>;
export default Observer;
