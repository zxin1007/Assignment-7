import { ComponentProps, ElementType } from 'react';
import { GifOverlayProps } from '../types';
import Video from './video';
declare type Props = {
    controls?: boolean;
    hideProgressBar?: boolean;
    hideMute?: boolean;
    hideAttribution?: boolean;
    persistentControls?: boolean;
    onUserMuted?: (muted: boolean) => void;
    overlay?: ElementType<GifOverlayProps>;
};
declare const VideoWrapper: (props: ComponentProps<typeof Video> & Props) => JSX.Element;
export default VideoWrapper;
